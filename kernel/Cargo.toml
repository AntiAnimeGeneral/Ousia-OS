[package]
name = "kernel"
version.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
bitflags = { version = "2", default-features = false }
ostd = { path = "../ostd" }

[features]
exception-smoke = ["ostd/exception-smoke"]
